# 应用服务 WEB 访问端口
server.port: 8083

spring:
  application.name: ai-qwen
  profiles:
    active: dev
  ai:
    dashscope:
      api-key: ${QWEN-API-KEY}
#      chat:
#        options:
#          model: qwen-image