## 1. DTO 类 — 字段级 Javadoc

- [x] 1.1 为 `AgentResponse.java` 所有字段添加中文 Javadoc
- [x] 1.2 为 `MemoryExtraction.java` 所有字段添加中文 Javadoc
- [x] 1.3 为 `SkillInfo.java` 所有字段添加中文 Javadoc

## 2. 配置类 — AgentConfig

- [x] 2.1 为 `AgentConfig.java` 的类和方法添加中文 Javadoc

## 3. 工具类 — AgentTools

- [x] 3.1 为 `AgentTools.java` 所有 public 方法添加中文 Javadoc（替换现有英文注释）
- [x] 3.2 为 `AgentTools.java` 的构造函数和 private 方法添加中文 Javadoc

## 4. 持久化服务 — MemoryStore 和 SkillManager

- [x] 4.1 为 `MemoryStore.java` 所有方法添加中文 Javadoc（替换现有英文注释）
- [x] 4.2 为 `SkillManager.java` 所有方法添加中文 Javadoc（替换现有英文注释）

## 5. 核心服务 — AgentService

- [x] 5.1 为 `AgentService.java` 所有 public/private 方法添加中文 Javadoc
- [x] 5.2 为 `AgentService.java` 内部类 `SkillGeneration` 添加中文 Javadoc

## 6. 控制器 — HermesAgentController

- [x] 6.1 为 `HermesAgentController.java` 所有 API 端点和辅助方法添加中文 Javadoc

## 7. 验证

- [x] 7.1 执行 `./mvnw -pl ai-deepseek compile` 验证编译通过
